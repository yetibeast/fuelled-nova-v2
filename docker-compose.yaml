services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    volumes:
      - './seeds:/app/seeds:ro'
    restart: unless-stopped

  frontend:
    build: ./frontend
    depends_on:
      - backend
    restart: unless-stopped
